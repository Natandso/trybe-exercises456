const numbers = [19, 21, 30, 3, 45, 22, 15];

// Adicione seu código aqui

numbers.find((numero) => numero % 3 === 0);

console.log(numbers.find((numero) => numero % 3 === 0));

console.log(numbers.find((numero) => numero % 5 === 0));